body {
    margin: 0;
    background: black;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: Arial, sans-serif;
}

/* Layout lado a lado */
.container {
    display: flex;
    align-items: center;
    gap: 60px;
}

/* Bolo */
.bolo {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    position: relative;
}

/* Camadas */
.camada {
